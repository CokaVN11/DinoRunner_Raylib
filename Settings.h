#pragma once
#ifndef SETTINGS_
#define SETTINGS_

const float ROOT_WIDTH = 800;
const float ROOT_HEIGHT = 400;
const float SCREEN_WIDTH = 800;
const float SCREEN_HEIGHT = 400;
const int FPS = 60;
inline int score = 0;

enum ObstacleType
{
	none, fly, ground
};

#endif // !SETTINGS_

